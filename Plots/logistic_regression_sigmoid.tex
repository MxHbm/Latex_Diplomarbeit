\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            axis lines=middle,
            axis line style = {-Stealth[{length=6pt}]},
            xlabel={$z$},
            ylabel={$s(z)$},
            xmin=-6, xmax=6,
            ymin=-0.2, ymax=1.2,
            samples=200,
            xtick={-6,-4,-2,0,2,4,6},
            ytick={0,0.5,1},
            width=10cm,
            height=6cm,
            ]
            % left half: red
            \addplot[red, thick, domain=-6:0] {1/(1+exp(-x))};

            % right half: green
            \addplot[green!70!black, thick, domain=0:6] {1/(1+exp(-x))};

            % text (use axis coordinates)
            \node[rectangle, minimum width=12pt, align=center]
            at (axis cs:-4,0.5) {\footnotesize Classified\\\footnotesize infeasible};

            % text (use axis coordinates)
            \node[rectangle, minimum width=12pt, align=center]
            at (axis cs:4,0.5) {\footnotesize Classified\\\footnotesize feasible};



            % dotted line y=1 (use axis coordinates)
            \addplot[dashed, gray] coordinates {(-6,1) (6,1)};
        \end{axis}
    \end{tikzpicture}
    \caption[Transformation of linear to logistic regression via the sigmoid function.]{Transformation of linear regression to logistic regression via the sigmoid function classified by threshold 0.5.}
    \label{fig:LR_plot}
\end{figure}